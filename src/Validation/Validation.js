import React from 'react';

const validation = (props) => {
    return(
        <div>
            Validation Here!
            <p>{props.inputLength}</p>
        </div>
    )
}

export default validation;